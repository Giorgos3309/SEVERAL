to format tis entolis set einai me autakia  , diladi:
set onoma="entoli"


paradigata entolwn:

set var="ls -l"
set var1="cat testfile.txt"
printlvars
var
var1
unset var
unset var1

ps axl | grep zombie

ls -a -l>>ls_file.txt
cat<ls_file.txt
ls>>ls_file.txt
ls>ls_file.txt

cd ../

exit
